package com.example.filemanager.ui.theme

// Тема для будущей реализации на Compose
// Будут использованы на Этапе 2: Compose автотесты

object AppTheme {
    // Конфигурация темы приложения
    const val THEME_NAME = "FileManagerTheme"

    // Стили для разных состояний
    object Styles {
        const val PRIMARY_BUTTON = "PrimaryButton"
        const val SECONDARY_BUTTON = "SecondaryButton"
        const val CARD_VIEW = "CardView"
    }
}